This project produces two artifacets:

-A probe that monitors the lifecycle of a flow.
-A plugin that exposes the database properties used by the probe.

In order to run the probe and plugin in the meandre application, -Create two databases
nema_flowresults and nema_jobstatus on the database server with the username and password (-look at the
flowresults.properties and jobstatus.properties file respectively.)



use the command below

java -classpath dist/meandre-server-1.4.5.jar:. org.meandre.executables.RunInfrastructureCMD -d .