<?xml version="1.0" encoding="UTF-8"?>
<flow xmlns="http://www.springframework.org/schema/webflow"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/webflow
        http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd">

	<input name="executableBundle" type="org.imirsel.nema.webapp.model.UploadedExecutableBundle" />

	<on-start>
		<set name="flowScope.supportedFiles" value="resource.getSupportedFileDataTypes()" />
		<set name="flowScope.supportedOs" value="resource.getSupportedOperatingSystems()" />
		<set name="flowScope.supportedGroups" value="resource.getSupportedGroups()" />
		<set name="flowScope.executable" value="tasksService.setExecutable(executableBundle)"/> 
	</on-start>

	<view-state id="upload" model="executable">
		<transition on="upload" to="arguments" >
		<set name="flowScope.paramTemplate" value="tasksService.getEmptyTemplate(executable.type)" />
		</transition>
		<transition on="cancel" to="cancel" validate="false" />
	</view-state>

	<view-state id="argumentsPlain">


		<transition on="cancel" to="cancel" />
		<transition on="back" to="upload" />
		<transition on="review" to="review">
			<evaluate
				expression="tasksService.setCommonTemplate(requestParameters,flowScope.paramTemplate)" />
			<evaluate
				expression="tasksService.generateExecutableBundle(flowScope.paramTemplate,executable)"/>
		</transition>
	</view-state>

	<view-state id="argumentsJava" model="paramTemplate">
		<transition on="cancel" to="cancel" />
		<transition on="back" to="upload" />
		<transition on="review" to="review">

			<evaluate
				expression="tasksService.setCommonTemplate(requestParameters,paramTemplate)" />
			<evaluate
				expression="tasksService.setparamTemplate(requestParameters,executable,paramTemplate)" />
			<evaluate
				expression="tasksService.generateExecutableBundle(paramTemplate,flowScope.executable)"/>

		</transition>
	</view-state>

	<view-state id="argumentsMatlab" model="flowScope.paramTemplate">

		<transition on="cancel" to="cancel" />
		<transition on="back" to="upload" />
		<transition on="review" to="review">

			<evaluate
				expression="tasksService.setCommonTemplate(requestParameters,paramTemplate)" />

			<evaluate
				expression="tasksService.generateExecutableBundle(paramTemplate,flowScope.executable)"
				 />
		</transition>
	</view-state>

	<view-state id="review">
		<transition on="cancel" to="cancel" />
		<transition on="back" to="arguments" />
		<transition on="save" to="success">
		</transition>
	</view-state>

	<action-state id="arguments">
		<evaluate expression="executable.type" />
		<transition on="JAVA" to="argumentsJava"/>
		<transition on="MATLAB" to="argumentsMatlab"/>
		<transition on="SHELL" to="argumentsPlain"/>
		<transition on="C" to="argumentsPlain"/>
	
	</action-state>

	<end-state id="cancel"></end-state>

	<end-state id="success">
		<output name="executableUrl" value="'LiteralValue'" type="java.lang.String" />
		<output name="executableOs" value="resource.getOsDataType(executable.preferredOs)"
			type="org.imirsel.nema.model.OsDataType" />
		<output name="executableGroup" value="executable.group" type="java.lang.String" />
		<output name="executableBundle" value="executable"
			type="org.imirsel.nema.model.ExecutableBundle" />


	</end-state>
</flow>